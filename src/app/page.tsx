import { Button } from "@/components/ui/button";
import { db } from "@/lib/db";

export default async function Home() {
  return (
    <main className="flex justify-around items-center h-screen">Home</main>
  );
}
